# ==========================================
# VARIABLES DE ENTORNO PARA PRODUCCIÓN (VERCEL)
# ==========================================

# ============= BASE DE DATOS =============
# IMPORTANTE: Cambiar "localhost" por la IP o dominio público de tu servidor MySQL
# Si usas cPanel, el host puede ser algo como: servidor123.tuhost.com
# Si es un servidor dedicado, usa la IP pública

# Formato: mysql://USUARIO:CONTRASEÑA@HOST:PUERTO/BASE_DE_DATOS
DATABASE_URL="mysql://transpo1_credenciales:feelthesky1@TU_HOST_MYSQL:3306/transpo1_credenciales"

# Alternativa con parámetros adicionales para mejor rendimiento:
# DATABASE_URL="mysql://transpo1_credenciales:feelthesky1@TU_HOST_MYSQL:3306/transpo1_credenciales?connection_limit=5&pool_timeout=30&connect_timeout=60"

# ============= SEGURIDAD =============
# JWT Secret - Generar uno nuevo con: openssl rand -base64 32
JWT_SECRET="GENERAR_UN_SECRET_ALEATORIO_LARGO_Y_SEGURO"

# ============= URL DE LA APLICACIÓN =============
# URL de tu aplicación en Vercel (se asignará automáticamente)
NEXT_PUBLIC_APP_URL="https://tu-proyecto.vercel.app"

# ============= CONFIGURACIÓN MYSQL ADICIONAL (OPCIONAL) =============
# Si tu servidor MySQL requiere SSL
# DATABASE_SSL="true"

# Si necesitas especificar el timezone
# DATABASE_TIMEZONE="America/Argentina/Buenos_Aires"
